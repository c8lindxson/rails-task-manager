<div class="container">
  <h1>Tasks</h1>
    <ul>
    <% @tasks.each do |t| %>
      <% if t.completed %>
        <li class="list-unstyled"><%= "☑" %>
        <%= link_to t.title, task_path(t.id) %></li>
      <% else %>
        <li class="list-unstyled"><%= "🔴" %>
        <%= link_to t.title, task_path(t.id)  %></li>
      <% end %>
    <% end %>
    </ul>

    <%= link_to "Make a new task", new_path %>
</div>
